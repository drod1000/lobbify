<header>

  <nav class="navbar navbar-inverse navbar-fixed-top navbar-pushin">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/"><img id='picture' src="http://www.clipartpanda.com/clipart_images/saves-you-money-and-time-41064225/download"/></a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-left">
          <li><%= link_to "View Cart", cart_path, class: "btn btn-default btn-sm" %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if current_user %>
            <li>Logged in as <%= current_user.name%></li>
            <li><%= link_to "Logout", logout_path, class: "btn btn-default btn-sm" %></li>
          <% else %>
            <li><%= link_to "Login", login_path, class: "btn btn-default btn-sm" %></li>
          <% end %>
          <li><%= link_to "Outings", outings_path, class: "btn btn-default btn-sm" %></li>
        </ul>
      </div>
    </div>
  </nav>
</header>
